server:
  port: 8081

spring:
  application:
    name: auth-service

funcionarios:
  service:
    url: ${FUNCIONARIOS_SERVICE_URL:http://localhost:8083}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
