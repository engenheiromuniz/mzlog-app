server:
  port: 8082

spring:
  application:
    name: clientes-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/clientes_db}
    username: ${SPRING_DATASOURCE_USERNAME:mzlog}
    password: ${SPRING_DATASOURCE_PASSWORD:mzlog}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
