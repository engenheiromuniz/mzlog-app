server:
  port: 8083

spring:
  application:
    name: funcionarios-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5434/funcionarios_db}
    username: ${SPRING_DATASOURCE_USERNAME:mzlog}
    password: ${SPRING_DATASOURCE_PASSWORD:mzlog}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
